# Music Playlist Explorer

A Python tool to fetch and filter music metadata from online playlists.

## Description
This script connects to music APIs to retrieve track information, processes the data in batches for efficiency, and uses Regular Expressions to filter titles based on character sets (specifically removing Chinese titles).

## Requirements
To run this script, you need to install the `requests` library:
```bash
pip install requests
